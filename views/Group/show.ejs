<%- include("../partials/header.ejs"); %>

<div class="jumbotron" style="background-color:<%= foundGroup.color%>">
	<div class="container" style="background: rgba(0,0,0,0.5); padding:20px">
		 <h1 style="text-shadow: #212022 5px 5px 10px;">
			<%=foundGroup.name %>
		</h1>
		<p style="text-shadow: #212022 5px 5px 10px;">
			<%=foundGroup.desc %>
		</p>
		<div class="btn-group">
			<a href="/index/<%=foundGroup._id %>/task" class="btn btn-sm btn-primary">Add Task/Note</a>
			<a href="/index/<%=foundGroup._id %>/edit" class="btn btn-sm btn-info">Edit Group</a>
			<a href="#" class="btn btn-sm btn-success">Add Member</a>
		</div>
		<form action="/index/<%=foundGroup._id%>?_method=DELETE" method="POST" style="display:inline-block;">
			<button class="btn btn-sm btn-danger">Delete Group</button>
		</form>
	</div>
</div>


<% foundGroup.Task.forEach(function(task){ %>
	<div>
		<h3 style="color:<%=task.color %>">
			<%= task.name %>	
		</h3>
		<p>
			<%= task.desc %><br>
			<% if(task.deadline){ %>
				<em>Deadline: <%= task.deadline %></em>
			<% } %>
		</p>
	</div>
<% }); %>


<%- include("../partials/footer.ejs"); %>